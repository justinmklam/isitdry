# isitdry

A crowd-sourced website to determine if it's dry enough to climb.

## Getting Started

Install locally and run tests:

```bash
make install

make test
```

To run the API:

```bash
make run
```
